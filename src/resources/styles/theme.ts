import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: { primary: { main: '#85C7F2' }, secondary: { main: '#636363' } },
  typography: {
    fontFamily: 'Inter',
  },
});

export default theme;
