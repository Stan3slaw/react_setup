version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install && nvm use
        - npm i
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
